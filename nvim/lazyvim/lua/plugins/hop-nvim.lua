-- Since flash.nvim is already providing the same functionality. No need for newer Plugin.
return {
  -- -- Better `movements`
  -- {
  --   'phaazon/hop.nvim',
  --   Verylazy = true,
  --   opts = {
  --     -- Options would go here
  --   },
  --   keys = {
  --     -- disable the keymap to grep files
  --     -- {"<leader>/", false},
  --     -- change a keymap
  --     { "hs",  "<cmd> HopChar2<CR>",    desc = "[H]op to two consecutive charachter" },
  --     { "hs", "<cmd> HopChar1<CR>",    desc = "[H]op to single charachter" },
  --     { "hw", "<cmd> HopWord<CR>",     desc = "[H]op to single word" },
  --     { "ha", "<cmd> HopAnywhere<CR>", desc = "[H]op [A]nywhere" },
  --   },
  --
  --   -- If activated this will not be lazy loaded.
  --   -- init = function()
  --   --   require('hop').setup(opts)
  --   -- end,
  -- }
}

