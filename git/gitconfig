# Remember global gitignore cant use relatively the other files. It needs to used with absolute path or
# path extended from home.
[core]
  # editor = /usr/bin/vim
  editor = $EDITOR

[init]
	defaultBranch = main

[credential]
	helper = cache --timeout=604800

[include]
  path = common_aliases
  path = ~/.gitconfig.local
